@model CarManager.Core.Dtos.CarDto

<h2>Edit Car</h2>

<form asp-action="Edit" method="post">
    @Html.HiddenFor(m => m.Id)
    @Html.EditorForModel()
    <button type="submit" class="btn btn-primary">Update</button>
</form>